[1mdiff --git a/src/lib/lipsync/phoneme-service.ts b/src/lib/lipsync/phoneme-service.ts[m
[1mindex c6f87af..02fbe94 100644[m
[1m--- a/src/lib/lipsync/phoneme-service.ts[m
[1m+++ b/src/lib/lipsync/phoneme-service.ts[m
[36m@@ -187,6 +187,14 @@[m [mexport class PhonemeService {[m
     durationSeconds: number,[m
     dialogText?: string[m
   ): Promise<VisemeTimeline> {[m
[32m+[m[32m    // O Rhubarb só aceita '.wav' e '.ogg' — o áudio do TTS chega em '.mp3',[m
[32m+[m[32m    // que ele rejeita com "Unsupported file extension" (erro que antes[m
[32m+[m[32m    // passava despercebido: caía silenciosamente no fallback heurístico,[m
[32m+[m[32m    // ver catch em extractVisemes). Converte pra um WAV temporário aqui e[m
[32m+[m[32m    // aponta o Rhubarb pra ele; o mp3 original não é tocado.[m
[32m+[m[32m    const wavPath = audioFilePath.replace(/\.(mp3|wav|ogg)$/i, ".rhubarb-input.wav");[m
[32m+[m[32m    await this.convertToWav(audioFilePath, wavPath);[m
[32m+[m
     const outputJsonPath = audioFilePath.replace(/\.(mp3|wav)$/i, ".rhubarb.json");[m
     // Recognizer "phonetic" (em vez do padrão "pocketSphinx", que só[m
     // reconhece inglês) — necessário porque a narração é em português.[m
[36m@@ -201,22 +209,39 @@[m [mexport class PhonemeService {[m
       await fs.writeFile(dialogPath, dialogText, "utf-8");[m
       args.push("-d", dialogPath);[m
     }[m
[31m-    args.push(audioFilePath);[m
[32m+[m[32m    args.push(wavPath);[m
 [m
[31m-    await this.runBinary(args);[m
[32m+[m[32m    try {[m
[32m+[m[32m      await this.runBinary(args);[m
 [m
[31m-    const raw = await fs.readFile(outputJsonPath, "utf-8");[m
[31m-    const parsed = JSON.parse(raw) as RhubarbOutput;[m
[31m-    const cues: VisemeCue[] = parsed.mouthCues.map((c) => ({[m
[31m-      start: c.start,[m
[31m-      end: c.end,[m
[31m-      shape: normalizeShape(c.value),[m
[31m-    }));[m
[32m+[m[32m      const raw = await fs.readFile(outputJsonPath, "utf-8");[m
[32m+[m[32m      const parsed = JSON.parse(raw) as RhubarbOutput;[m
[32m+[m[32m      const cues: VisemeCue[] = parsed.mouthCues.map((c) => ({[m
[32m+[m[32m        start: c.start,[m
[32m+[m[32m        end: c.end,[m
[32m+[m[32m        shape: normalizeShape(c.value),[m
[32m+[m[32m      }));[m
 [m
[31m-    await fs.unlink(outputJsonPath).catch(() => undefined);[m
[31m-    if (dialogPath) await fs.unlink(dialogPath).catch(() => undefined);[m
[32m+[m[32m      return { sceneId, durationSeconds, cues, source: "rhubarb" };[m
[32m+[m[32m    } finally {[m
[32m+[m[32m      await fs.unlink(wavPath).catch(() => undefined);[m
[32m+[m[32m      await fs.unlink(outputJsonPath).catch(() => undefined);[m
[32m+[m[32m      if (dialogPath) await fs.unlink(dialogPath).catch(() => undefined);[m
[32m+[m[32m    }[m
[32m+[m[32m  }[m
 [m
[31m-    return { sceneId, durationSeconds, cues, source: "rhubarb" };[m
[32m+[m[32m  /** Converte qualquer áudio suportado pelo ffmpeg para um WAV PCM 16-bit[m
[32m+[m[32m   *  mono — formato que o Rhubarb aceita e que também é o mais rápido pra[m
[32m+[m[32m   *  ele processar (não precisa decodificar mp3 internamente). */[m
[32m+[m[32m  private convertToWav(inputPath: string, outputPath: string): Promise<void> {[m
[32m+[m[32m    return new Promise((resolve, reject) => {[m
[32m+[m[32m      ffmpeg(inputPath)[m
[32m+[m[32m        .outputOptions(["-ac", "1", "-ar", "16000", "-c:a", "pcm_s16le"])[m
[32m+[m[32m        .output(outputPath)[m
[32m+[m[32m        .on("end", () => resolve())[m
[32m+[m[32m        .on("error", reject)[m
[32m+[m[32m        .run();[m
[32m+[m[32m    });[m
   }[m
 [m
   private runBinary(args: string[]): Promise<void> {[m
[1mdiff --git a/src/lib/render.ts b/src/lib/render.ts[m
[1mindex 0657c05..80302a4 100644[m
[1m--- a/src/lib/render.ts[m
[1m+++ b/src/lib/render.ts[m
[36m@@ -94,7 +94,19 @@[m [mfunction buildIsSpeakingExpr(silences: SilenceInterval[]): string {[m
  * era hardcoded pros 3 estados da Fase 1 — closed/half/open) pra não[m
  * precisar tocar aqui de novo se o número de bocas mudar outra vez.[m
  */[m
[31m-function buildMouthEnableExpressions(cues: MouthCue[]): Record<MouthState, string> {[m
[32m+[m[32m/**[m
[32m+[m[32m * Recebe a lista de estados de boca a considerar (normalmente só os que[m
[32m+[m[32m * aparecem de fato nas `cues` da cena — ver `getUsedMouthStates` — em vez[m
[32m+[m[32m * de sempre os 8 estados existentes) e monta a expressão `enable` de cada[m
[32m+[m[32m * um. Manter essa lista enxuta é o que evita montar 8 cadeias completas de[m
[32m+[m[32m * scale+rotate+overlay no filter_complex quando a cena só usa 2 ou 3[m
[32m+[m[32m * bocas — cada estado fora da lista simplesmente não gasta memória/CPU do[m
[32m+[m[32m * ffmpeg, já que nem chega a virar input/filtro.[m
[32m+[m[32m */[m
[32m+[m[32mfunction buildMouthEnableExpressions([m
[32m+[m[32m  cues: MouthCue[],[m
[32m+[m[32m  states: MouthState[][m
[32m+[m[32m): Record<MouthState, string> {[m
   const safeCues = cues.length <= MAX_MOUTH_CUES_IN_FILTER ? cues : [];[m
 [m
   const sumFor = (state: MouthState): string => {[m
[36m@@ -106,24 +118,41 @@[m [mfunction buildMouthEnableExpressions(cues: MouthCue[]): Record<MouthState, strin[m
   };[m
 [m
   const sums = Object.fromEntries([m
[31m-    MOUTH_ASSET_KEYS.map((key) => [key, sumFor(key)])[m
[32m+[m[32m    states.map((key) => [key, sumFor(key)])[m
   ) as Record<MouthState, string>;[m
[31m-  const totalSum = MOUTH_ASSET_KEYS.map((key) => sums[key]).join("+");[m
[32m+[m[32m  const totalSum = states.map((key) => sums[key]).join("+");[m
 [m
   const exprs = {} as Record<MouthState, string>;[m
[31m-  for (const key of MOUTH_ASSET_KEYS) {[m
[32m+[m[32m  for (const key of states) {[m
     exprs[key] =[m
       key === "closed"[m
         ? // "closed" também é o padrão de repouso: fica ativo se[m
[31m-          // explicitamente marcado OU se nenhum dos 8 estados cobre o[m
[31m-          // instante t (ex.: cue ausente por causa do limite de segurança[m
[31m-          // acima).[m
[32m+[m[32m          // explicitamente marcado OU se nenhum dos estados presentes cobre[m
[32m+[m[32m          // o instante t (ex.: cue ausente por causa do limite de segurança[m
[32m+[m[32m          // acima, ou estado sem nenhuma cue nessa cena).[m
           `gt(${sums.closed},0)+eq(${totalSum},0)`[m
         : `gt(${sums[key]},0)`;[m
   }[m
   return exprs;[m
 }[m
 [m
[32m+[m[32m/**[m
[32m+[m[32m * Estados de boca que realmente aparecem nas cues dessa cena, na ordem[m
[32m+[m[32m * fixa de MOUTH_ASSET_KEYS, sempre incluindo "closed" (fallback de[m
[32m+[m[32m * repouso/default mesmo que nenhuma cue "closed" explícita exista).[m
[32m+[m[32m * Cenas curtas tipicamente usam só 2-4 dos 8 estados — restringir a isso[m
[32m+[m[32m * evita montar as cadeias de filtro (e os inputs de imagem correspondentes)[m
[32m+[m[32m * dos estados não usados, reduzindo bastante o consumo de memória do[m
[32m+[m[32m * ffmpeg por cena.[m
[32m+[m[32m */[m
[32m+[m[32mfunction getUsedMouthStates(cues: MouthCue[]): MouthState[] {[m
[32m+[m[32m  const used = new Set<MouthState>(["closed"]);[m
[32m+[m[32m  if (cues.length <= MAX_MOUTH_CUES_IN_FILTER) {[m
[32m+[m[32m    for (const c of cues) used.add(c.state);[m
[32m+[m[32m  }[m
[32m+[m[32m  return MOUTH_ASSET_KEYS.filter((key) => used.has(key));[m
[32m+[m[32m}[m
[32m+[m
 /**[m
  * Confere que uma expressão de filtro do FFmpeg está com os parênteses[m
  * balanceados antes de mandar pro ffmpeg.[m
[36m@@ -220,17 +249,22 @@[m [mexport async function renderSceneClip(scene: RenderScene, index: number): Promis[m
   const mouthFrames = await getMascotFrames();[m
 [m
   const duration = await getAudioDuration(scene.audioPath);[m
[31m-  const mouthEnableExpr = buildMouthEnableExpressions(scene.mouthCues);[m
[32m+[m[32m  // Só os estados de boca que a cena realmente usa (tipicamente 2-4 dos 8) —[m
[32m+[m[32m  // ver getUsedMouthStates. Evita montar inputs/filtros pros estados que[m
[32m+[m[32m  // essa cena nunca precisa, principal causa do consumo alto de memória do[m
[32m+[m[32m  // ffmpeg nessa etapa.[m
[32m+[m[32m  const usedStates = getUsedMouthStates(scene.mouthCues);[m
[32m+[m[32m  const mouthEnableExpr = buildMouthEnableExpressions(scene.mouthCues, usedStates);[m
   const rotateExpr = headMotion.buildFfmpegRotateExpr();[m
 [m
   const mascotX = `W-w-${MASCOT_MARGIN}`;[m
   const mascotY = `${MASCOT_MARGIN}`; // canto SUPERIOR direito[m
 [m
   // Índices dos inputs do ffmpeg: 0 = imagem da cena, 1 = áudio,[m
[31m-  // 2..9 = os 8 estados de boca, na ordem de MOUTH_ASSET_KEYS.[m
[32m+[m[32m  // 2..N = os estados de boca usados nessa cena, na ordem de MOUTH_ASSET_KEYS.[m
   const MOUTH_INPUT_BASE = 2;[m
   const mouthInputIdx = Object.fromEntries([m
[31m-    MOUTH_ASSET_KEYS.map((key, i) => [key, MOUTH_INPUT_BASE + i])[m
[32m+[m[32m    usedStates.map((key, i) => [key, MOUTH_INPUT_BASE + i])[m
   ) as Record<MouthState, number>;[m
 [m
   let zoomExpr: string;[m
[36m@@ -259,7 +293,7 @@[m [mexport async function renderSceneClip(scene: RenderScene, index: number): Promis[m
   assertBalancedExpr("zoompan:z", zoomExpr);[m
   assertBalancedExpr("zoompan:x", xExpr);[m
   assertBalancedExpr("zoompan:y", yExpr);[m
[31m-  for (const key of MOUTH_ASSET_KEYS) {[m
[32m+[m[32m  for (const key of usedStates) {[m
     assertBalancedExpr(`overlay:enable(${key})`, mouthEnableExpr[key]);[m
   }[m
   assertBalancedExpr("rotate:a", rotateExpr);[m
[36m@@ -299,8 +333,9 @@[m [mexport async function renderSceneClip(scene: RenderScene, index: number): Promis[m
       inputs: "bg2",[m
       outputs: "bg",[m
     },[m
[31m-    // Os 8 frames de boca do mascote, escalados pro tamanho final da bolha.[m
[31m-    ...MOUTH_ASSET_KEYS.map((key) => ({[m
[32m+[m[32m    // Frames de boca do mascote realmente usados nessa cena, escalados pro[m
[32m+[m[32m    // tamanho final da bolha (ver usedStates acima).[m
[32m+[m[32m    ...usedStates.map((key) => ({[m
       filter: "scale",[m
       options: { w: MASCOT_DISPLAY_SIZE, h: MASCOT_DISPLAY_SIZE },[m
       inputs: `${mouthInputIdx[key]}:v`,[m
[36m@@ -314,7 +349,7 @@[m [mexport async function renderSceneClip(scene: RenderScene, index: number): Promis[m
     // circular, ver mascot.tsx), e `ow=iw:oh=ih` mantém o tamanho do[m
     // canvas igual ao original (rotação pequena o suficiente pra não[m
     // cortar a bolha).[m
[31m-    ...MOUTH_ASSET_KEYS.map((key) => ({[m
[32m+[m[32m    ...usedStates.map((key) => ({[m
       filter: "rotate",[m
       options: { a: rotateExpr, c: "none", ow: "iw", oh: "ih" },[m
       inputs: key,[m
[36m@@ -324,9 +359,9 @@[m [mexport async function renderSceneClip(scene: RenderScene, index: number): Promis[m
     // base/fallback), os outros 7 por cima. Como as expressões `enable` são[m
     // mutuamente exclusivas (ver buildMouthEnableExpressions), só uma[m
     // camada fica visível em cada instante t.[m
[31m-    ...MOUTH_ASSET_KEYS.map((key, i) => {[m
[32m+[m[32m    ...usedStates.map((key, i) => {[m
       const prevOut = i === 0 ? "bg" : "ov" + (i - 1);[m
[31m-      const outName = i === MOUTH_ASSET_KEYS.length - 1 ? "vout" : "ov" + i;[m
[32m+[m[32m      const outName = i === usedStates.length - 1 ? "vout" : "ov" + i;[m
       return {[m
         filter: "overlay",[m
         options: { x: mascotX, y: mascotY, enable: mouthEnableExpr[key] },[m
[36m@@ -337,7 +372,7 @@[m [mexport async function renderSceneClip(scene: RenderScene, index: number): Promis[m
   ];[m
 [m
   let command = ffmpeg().input(scene.imagePath).inputOptions(["-loop 1"]).input(scene.audioPath);[m
[31m-  for (const key of MOUTH_ASSET_KEYS) {[m
[32m+[m[32m  for (const key of usedStates) {[m
     command = command.input(mouthFrames[key]).inputOptions(["-loop 1"]);[m
   }[m
 [m
